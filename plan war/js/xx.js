/* var enemyTime = 2000;
setTime = setInterval(setstar, 2000);
function setstar() {
    star = document.createElement('div');
    star.setAttribute('id', 'star');
    main.appendChild(star);
    var i = Math.floor(Math.random * 12) * star.offsetWidth;
    
} */

function initGame() {
    var star = [];
}